// Hello, World! in Tuff
// Demonstrates basic I/O and function definition

extern {
    fn puts(s: &str) -> i32;
    fn exit(code: i32) -> void;
}

fn main() -> void {
    puts("Hello, World!");
}

