fn count_to_n(n) {
  for (let i in 0..n) {
    print(i)
  }
}

fn sum_range(start, end) {
  var sum = 0
  for (let i in start..end) {
    sum = sum + i
  }
  return sum
}

print("Counting to 5:")
count_to_n(5)

print("Sum from 0 to 10:")
print(sum_range(0, 10))
