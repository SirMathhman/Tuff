out struct Point {
    x: I32,
    y: I32
}

out fn add(a: I32, b: I32): I32 => a + b;

fn privateHelper(): I32 => 42;

out fn main(): I32 => {
    let p = Point { 10, 20 };
    return add(p.x, p.y) + privateHelper();
}
