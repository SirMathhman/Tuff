{
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "cmd /c \"bun test --coverage --coverage-reporter=lcov 1>&2\"; if ($LASTEXITCODE -ne 0) { exit 2 }"
      }
    ]
  }
}
