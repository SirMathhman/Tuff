{
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "cmd /c \"bun test --coverage 1>&2\"; if ($LASTEXITCODE -ne 0) { Write-Error \"If coverage is unclear, check ./coverage/lcov.info\"; exit 2 }"
      }
    ]
  }
}
