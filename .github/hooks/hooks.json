{
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "cmd /c \"cargo llvm-cov --fail-under-lines 95 --show-missing-lines 1>&2\"; if ($LASTEXITCODE -ne 0) { exit 2 }"
      }
    ]
  }
}
