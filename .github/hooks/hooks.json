{
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "cmd /c \"bun test 1>&2\"; if ($LASTEXITCODE -ne 0) { exit 2 }"
      },
      {
        "type": "command",
        "command": "cmd /c \"pmd cpd index.js index.test.js --language ecmascript --minimum-tokens 50 --ignore-literals --ignore-identifiers 1>&2\"; if ($LASTEXITCODE -ne 0) { exit 2 }"
      }
    ]
  }
}
