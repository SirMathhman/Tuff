{
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "cmd /c \"powershell -NoProfile -File ./.github/hooks/hooks.ps1 1>&2\"; if ($LASTEXITCODE -ne 0) { exit 2 }"
      }
    ]
  }
}
