/**
 * Stub implementation of `interpret`.
 * Accepts a string and returns a number. Implement behavior later.
 */
export function interpret(input: string): number {
  // TODO: implement proper parsing/interpretation logic
  return 0;
}
