// Parser error handling

// Parser error with location information
struct ParseError {
	line: USize,
	column: USize,
	message: string
}

impl ParseError {
	fn new(line: USize, column: USize, message: string): ParseError => {
		return ParseError { line, column, message };
	}
}
