struct Point {
    x: I32,
    y: I32
}

impl Point {
    fn new(x: I32, y: I32): Point => {
        yield Point { x: x, y: y };
    }

    fn sum(p: Point): I32 => p.x + p.y;
}

let p = Point.new(10, 20);
let result = Point.sum(p);

// The last expression determines the exit code
30
