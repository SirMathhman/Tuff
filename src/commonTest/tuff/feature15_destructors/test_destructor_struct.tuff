// Test destructor with struct type

struct Resource {
    id: I32,
    size: I32
}

fn freeResource(res: Resource): Void => {
    // In a real scenario, this would free the resource
}

fn test(): I32 => {
    let r: Resource & #freeResource = Resource { 1, 100 };
    r.id + r.size
}

test()
