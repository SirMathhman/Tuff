// Test basic destructor attached via intersection type
// The destructor should be called when the variable goes out of scope

fn myDestructor(val: I32): Void => {
    // In a real scenario, this would free resources
}

fn test(): I32 => {
    let x: I32 & ~myDestructor = 42;
    x
}

test()
