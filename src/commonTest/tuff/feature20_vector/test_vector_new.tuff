// Test basic vector creation and length

fn run(): I32 => {
    let vec: Vector<I32> = vectorNew<I32>();
    
    if (vectorLen<I32>(vec) != 0) {
        return 1;
    }
    
    if (!vectorIsEmpty<I32>(vec)) {
        return 2;
    }
    
    return 0;
}

run()
