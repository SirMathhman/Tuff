fn a(ref : I32) => {
    fn b() => {
        fn c() => {
            // this is scope of c, this.this is scope of b, this.this.this is scope of a
            this.this.this.ref
        }

        c()
    }

    b()
}

a(100)
// Should print out 100

