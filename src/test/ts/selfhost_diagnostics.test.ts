import { describe, test } from "vitest";

// Moved to `src/test/ts/api/selfhost_diagnostics.test.ts`.
describe("migrated test file", () => {
  test.skip("selfhost_diagnostics moved to api suite", () => {
    // Intentionally empty.
  });
});
