import { describe, test } from "vitest";

// Moved to `src/test/ts/api/selfhost_any_invalid_type.test.ts`.
describe("migrated test file", () => {
  test.skip("selfhost_any_invalid_type moved to api suite", () => {
    // Intentionally empty.
  });
});
