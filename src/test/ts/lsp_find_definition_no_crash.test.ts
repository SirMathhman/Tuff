import { describe, test } from "vitest";

// Moved to `src/test/ts/api/lsp_find_definition_no_crash.test.ts`.
describe("migrated test file", () => {
  test.skip("lsp_find_definition_no_crash moved to api suite", () => {
    // Intentionally empty.
  });
});
