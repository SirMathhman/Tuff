import { describe, test } from "vitest";

// Moved to `src/test/ts/api/selfhost_deprecation_warnings.test.ts`.
describe("migrated test file", () => {
  test.skip("selfhost_deprecation_warnings moved to api suite", () => {
    // Intentionally empty.
  });
});
