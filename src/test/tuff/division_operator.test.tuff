from std::test use { reset, suite, it, expect_eq, summary, status };

fn main() : I32 => {
  reset();
  suite("division operator");

  it("8 / 2", expect_eq("8/2", 8 / 2, 4));
  it("9 / 3", expect_eq("9/3", 9 / 3, 3));

  summary();
  status()
}
