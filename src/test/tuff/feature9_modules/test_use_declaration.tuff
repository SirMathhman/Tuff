// Test: Use declaration for importing modules
// Test using module-qualified calls without use declaration

module com::utils {
    fn increment(x: I32): I32 => x + 1;
}

let result = com::utils::increment(5);
result
