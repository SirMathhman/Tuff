// Test: expect/actual declarations inside modules
// The FQN comes from the module context, not the function name

module math {
    expect fn doubleIt(x: I32): I32;
}

module math {
    actual fn doubleIt(x: I32): I32 => x * 2;
}

// Call the function using FQN
math::doubleIt(21)



