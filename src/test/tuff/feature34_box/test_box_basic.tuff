// Test basic Box<T> functionality - owned heap allocation

use mem;

fn main(): I32 => {
	// Create a boxed integer
	let b: Box<I32> = Box::new<I32>(42);
	
	// Access the value
	let val: I32 = b.get();
	
	if (val == 42) {
		return 0;
	}
	return 1;
}
