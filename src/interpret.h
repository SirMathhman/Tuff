#ifndef INTERPRET_H
#define INTERPRET_H

#include <stddef.h>

// interpret - stub function that takes an input string and returns a newly
// allocated output string. The caller is responsible for freeing the result.
char *interpret(const char *input);

#endif // INTERPRET_H
