// Example demonstrating method calls with nested function calls
fn Math() => {
    fn square(x : I32) => x * x;
    
    fn add(x : I32) => {
        square(this.getValue())
    }
    
    fn getValue() => 5;
    
    this
}

Math().add(0)

