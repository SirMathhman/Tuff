// Test: Struct with nested struct field
struct Inner {
    value: I32
}

struct Outer {
    inner: Inner,
    other: I32
}

let i = Inner { 42 };
let o = Outer { i, 100 };

let val = o.inner.value;
// Expected: val = 42
