// Test: Nested control flow
let mut x: I32 = 0;
let mut y: I32 = 0;

while (x < 3) {
    if (x == 1) {
        y = y + 10;
    } else {
        y = y + 1;
    }
    x = x + 1;
}

// Expected: x = 3, y = 12 (1+10+1)
