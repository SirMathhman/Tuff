# Tuff

Minimal Maven project generated and configured to compile with Java 24.

Build and run:

- Build: mvn -v && mvn clean package
- Run: java -jar target/tuff-0.1.0-SNAPSHOT.jar (if you add a maven-jar-plugin configuration)
- Test: mvn test
