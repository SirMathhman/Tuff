# Tuff Bun Project

Small demo project scaffolded to run with Bun.

Commands

- Install dependencies (if you need to add any): `bun install`
- Run tests: `bun test`
- Run app: `bun run src/index.ts` or `bun start`
