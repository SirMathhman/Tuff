# Tuff

A small TypeScript interpreter/evaluator used for experimenting with expressions, sized integer suffixes, blocks, assignments, and while-loops.

## Development

- Run tests: `pnpm test`
- Run lint: `pnpm lint`

The test suite is written with Vitest and lives under `test/`.
