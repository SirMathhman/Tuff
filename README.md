# Tuff — TypeScript starter

Basic TypeScript project scaffolded with pnpm, ESLint, Prettier, Jest, Husky and lint-staged.

Quick commands:

- Install (already done): `pnpm install`
- Run tests: `pnpm test`
- Lint: `pnpm lint`
- Format: `pnpm format`
- Build: `pnpm build`
- Dev: `pnpm dev`
