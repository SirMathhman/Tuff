# Tuff — Simple Maven project

This is a tiny Maven project created to match the local JVM version.

Build & test

```
mvn -v
mvn -DskipTests=false test
```

The project is configured to use Java release 24 (detected from `java --version`).
