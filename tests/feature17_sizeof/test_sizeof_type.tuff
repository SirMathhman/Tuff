fn getSize() -> SizeOf<I32> {
    return sizeOf(I32);
}

fn takeSize(u: USize) {
}

fn main() -> I32 {
    let s: SizeOf<I32> = sizeOf(I32);
    let u: USize = s;
    let x: USize = s + 1;
    
    takeSize(s);
    takeSize(sizeOf(I32));
    
    let s2: SizeOf<I32> = getSize();
    
    return 0;
}
