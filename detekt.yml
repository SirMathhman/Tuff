processors:
  active: true

config:
  validation: true

custom-rules:
  active: true
  NoThrow:
    active: true
  FunctionLength:
    active: true
    maxLines: 50

style:
  ReturnCount:
    active: false
