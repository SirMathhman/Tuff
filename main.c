#include <stdio.h>
#include <string.h>
#include <stdbool.h>

typedef struct {
		int x;
} Wrapper;

int unwrap(Wrapper wrapper);


int unwrap(Wrapper wrapper) {
		return ();
}int main() {
	
	return 0;
}
