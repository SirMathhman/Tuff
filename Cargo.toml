[package]
name = "tuff"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "repl"
path = "src/bin/repl.rs"
test = false

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(coverage)'] }

[dependencies]
