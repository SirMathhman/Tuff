{
  "name": "tuff",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "check": "pnpm run test && pnpm run cpd && pnpm run lint",
    "cpd": "pmd cpd src tests --language typescript --minimum-tokens 35 --format markdown",
    "format": "prettier --write .",
    "lint": "tsc --noEmit && eslint .",
    "prepare": "husky",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.1.0",
    "typescript": "^5.2.2",
    "vitest": "^1.0.0"
  },
  "module": "index.ts",
  "type": "module",
  "dependencies": {
    "@eslint/js": "^9.39.2",
    "eslint": "^9.39.2",
    "globals": "^17.2.0",
    "husky": "^9.1.7",
    "jiti": "^2.6.1",
    "typescript-eslint": "^8.54.0"
  }
}
