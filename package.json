{
  "name": "tuff-bun-project",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun run tsc && bun test --all",
    "start": "bun run src/index.ts",
    "tsc": "tsc -p . --noEmit",
    "prepare": "husky install"
  },
  "devDependencies": {
    "bun-types": "^1.3.3",
    "husky": "^8.0.0",
    "typescript": "^5.4.0"
  }
}
