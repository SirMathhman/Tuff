{
  "name": "tuff",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "lint:eslint": "eslint .",
    "analyze:pmd-cpd": "pmd cpd -d src,test -l typescript --minimum-tokens=100 --no-fail-on-error",
    "check:clutter": "bun run scripts/check-clutter.ts",
    "madge:visualize": "madge ./src --extensions ts --image ./docs/graph.svg ",
    "madge:circular" : "madge ./src --extensions ts --circular"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "eslint": "^10.8.1",
    "typescript-eslint": "^8.67.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "madge": "^8.0.0"
  }
}
