{
  "name": "tuff",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "pwsh ./scripts/test.ps1",
    "lint": "tsc --noEmit && eslint .  --fix",
    "format": "prettier . --write",
    "prepare": "husky",
    "cpd": "pmd cpd --dir src --language typescript --minimum-tokens 50 --ignore-identifiers --ignore-literals",
    "circular": "madge ./src --circular --extensions ts"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/bun": "latest",
    "eslint": "^10.8.0",
    "globals": "^17.8.0",
    "husky": "^9.1.7",
    "jiti": "^2.7.0",
    "madge": "^8.0.0",
    "prettier": "^3.9.6",
    "typescript-eslint": "^8.65.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
