{
  "name": "tuff",
  "module": "./src/index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "find-duplicates": "pmd cpd src tests --language typescript --minimum-tokens 35 --ignore-literals",
    "lint": "tsc --noEmit && eslint . --ext .ts",
    "test": "vitest run",
    "prepare": "husky"
  },
  "dependencies": {
    "@typescript-eslint/parser": "^8.54.0",
    "eslint": "^9.39.2"
  }
}
