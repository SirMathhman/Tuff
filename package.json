{
  "name": "tuff",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watchAll",
    "build": "tsc --build",
    "cpd": "pmd cpd --minimum-tokens 50 src --language typescript --format text --ignore-literals --ignore-identifiers",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/jest": "^29.5.3",
    "husky": "^9.1.7",
    "jest": "^29.6.1",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.3"
  }
}
