{
  "name": "tuff",
  "module": "./src/main/js/index.js",
  "private": true,
  "scripts": {
    "watch": "nodemon",
    "lint": "eslint . --fix",
    "test": "bun test",
    "cpd": "pmd cpd --dir src --language ecmascript --minimum-tokens 50 --ignore-identifiers --ignore-literals",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.7.0",
    "globals": "^17.7.0",
    "nodemon": "^3.1.14",
    "prettier": "^3.9.6"
  }
}
