{
  "name": "tuff",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "cpd": "pmd cpd --minimum-tokens 50 --ignore-literals --ignore-identifiers src tests --language typescript",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
