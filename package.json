{
  "name": "tuff-bun-project",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun run tsc && bun test --all",
    "start": "bun run src/index.ts",
    "tsc": "tsc -p . --noEmit",
    "lint": "eslint . --ext .ts --max-warnings=0",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.48.1",
    "@typescript-eslint/parser": "^8.48.1",
    "bun-types": "^1.3.3",
    "eslint": "^9.39.1",
    "husky": "^8.0.0",
    "typescript": "^5.4.0"
  }
}
