{
  "name": "tuff",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.8.1"
  },
  "scripts": {
    "cpd": "pmd cpd src tests --language typescript --minimum-tokens 35 --ignore-literals --ignore-identifiers",
    "lint": "tsc --noEmit",
    "format": "prettier . --write",
    "prepare": "husky"
  }
}
