#!/bin/sh
#!/bin/sh
# Run mvn verify (tests + checks) before committing; abort commit on failure.
echo "Running mvn -q verify before commit..."
if mvn -q verify; then
  echo "Verify passed — continuing commit."
  exit 0
else
  echo "Verify failed — aborting commit." 1>&2
  exit 1
fi
