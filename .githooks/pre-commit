#!/bin/bash
# Git pre-commit hook - Delegates to Python script for cross-platform support

# Get the directory where this script is located
HOOKS_DIR="$(git rev-parse --git-dir)/../.githooks"
HOOKS_DIR="$(cd "$HOOKS_DIR" && pwd)"

# Execute the Python hook
python3 "$HOOKS_DIR/pre-commit.py"
exit $?
