use string_builder;
use string;
use io;

let mut sb = StringBuilder::new();
sb.append("Hello");
sb.append(", ");
sb.append("World!");

let s = sb.toString();
println(s);

if (string_equals(s, "Hello, World!")) {
	println("Test Passed");
} else {
	println("Test Failed");
}

sb.free();
